<UserControl x:Class="MAMEUtility.MAMEUtilitySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">

    <StackPanel Orientation="Vertical">


        <GroupBox Header="MAME romset information source" Margin="15">

            <StackPanel Margin="10">
                <Label FontWeight="Bold">Select MAME romset information type source</Label>
                <RadioButton 
                    x:Name="RadioButton_UseMameExecutable"
                    Margin="5" 
                    IsChecked="{Binding Settings.UseMameExecutable}" 
                    Checked="RadioButton_UseMameExecutable_Checked">
                        Use MAME executable (.exe)
                </RadioButton>
                <RadioButton
                    x:Name="RadioButton_UseMameFile"
                    Margin="5" 
                    IsChecked="{Binding Settings.UseMameListFile}" 
                    Checked="RadioButton_UseMameFile_Checked">
                        Use MAME list file (.xml|.dat)
                </RadioButton>

                <StackPanel 
                        Orientation="Vertical" 
                        Margin="0 0 0 0"    
                        IsEnabled="{Binding ElementName=RadioButton_UseMameExecutable, Path=IsChecked}">
                        <TextBox
                            x:Name="MameExecutable_TextBox"
                            TextWrapping="Wrap" 
                            Margin="0,10,0,0" 
                            Text="{Binding Settings.MameExecutableFilePath}" />
                        <Button Content="Select MAME Executable" Width="Auto" Margin="0,10,0,0" Click="Button_SelectMameExecutable"/>
                </StackPanel>

                <StackPanel 
                        Orientation="Vertical" 
                        Margin="0 10 0 0"
                        IsEnabled="{Binding ElementName=RadioButton_UseMameFile, Path=IsChecked}">
                        <TextBox
                            x:Name="GamelistXML_TextBox"
                            TextWrapping="Wrap" 
                            Margin="0,10,0,0" 
                            Text="{Binding Settings.MameListFilePath}" />
                        <Button Content="Select MAME Gamelist XML file" Width="Auto" Margin="0,10,0,0" Click="Button_SelectMameGamelistXmlFilePath"/>
                </StackPanel>
                
            </StackPanel>
        
        </GroupBox>

    </StackPanel>

</UserControl>